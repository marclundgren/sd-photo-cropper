```sh
# Create a new React project with TypeScript
npx create-react-app photo-editor --template typescript

# Navigate to the project directory
cd photo-editor

# Install necessary dependencies
npm install react-dropzone react-image-crop file-saver jszip @types/file-saver @types/jszip
```